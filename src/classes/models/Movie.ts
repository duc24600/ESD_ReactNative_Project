class Movie {
    id : string;
    title : string;
    releaseYear : string;

    constructor(id: string, title: string, releaseYear: string ) {
        this.id = id;
        this.releaseYear = releaseYear;
        this.title = title;
    }
}